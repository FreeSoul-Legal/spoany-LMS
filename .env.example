# Supabase 프로젝트 연결 정보 (Project Settings → API 에서 확인)
# 로그인 화면이 없는 단일 사용자 도구라, 공개용 publishable(anon) 키만 있으면 된다.
# service_role 키는 필요 없다(오히려 사용하면 안 된다).
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Claude API 키 (사안 분석/서면 작성용, 서버에서만 사용)
ANTHROPIC_API_KEY=
