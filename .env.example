# Supabase 프로젝트 연결 정보 (Project Settings → API 에서 확인)
# 로그인 화면이 없는 단일 사용자 도구이므로, DB 접근은 서버에서 서비스 롤 키로만 수행한다.
# SUPABASE_SERVICE_ROLE_KEY는 절대 브라우저로 노출되면 안 되므로 VITE_ 접두사를 붙이지 않는다.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Claude API 키 (사안 분석/서면 작성용, 서버에서만 사용)
ANTHROPIC_API_KEY=
